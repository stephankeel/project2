import { BlindListentryFormatterPipe } from './blind-listentry-formatter.pipe';

describe('BlindListentryFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new BlindListentryFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
