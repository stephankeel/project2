import { HumidityListitemFormatterPipe } from './humidity-listitem-formatter.pipe';

describe('HumidityListitemFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new HumidityListitemFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
